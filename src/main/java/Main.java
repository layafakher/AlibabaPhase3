import Model.*;
import Repository.DAO.RepositoryFacade;

import java.util.*;

public class Main {

    public static void main(String[] args) {

//        RepositoryFacade.getInstance().deleteByID(4L, Airport.class);
//        RepositoryFacade.getInstance().findByIDs(Arrays.asList(2L,5L,7L), User.class).forEach(x -> System.out.println(x.getName() + " " + x.getLastName()));
//        System.out.println(RepositoryFacade.getInstance().findById(789L, Hotel.class).map(Hotel::getName));
//        System.out.println(RepositoryFacade.getInstance().deleteByIDs(Arrays.asList(3L,7L), Room.class));
////        RepositoryFacade.getInstance().findAll(AirlineCompany.class).forEach(x -> System.out.println(x.getAirlineName()));
//        User user = RepositoryFacade.getInstance().findById(3L,User.class).get();
//        user.setSignUpDate(new Date());
//        System.out.println(RepositoryFacade.getInstance().save(user,User.class).getSignUpDate());
    }
}
